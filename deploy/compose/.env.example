# PostgreSQL Configuration (DEV ONLY - change for production)
KC_DB_PASSWORD=changeme
KC_DB_USER=keycloak
KC_DB_NAME=keycloak

# Keycloak Admin Configuration (DEV ONLY - change for production)
KC_ADMIN_PASSWORD=changeme
KC_ADMIN_USER=admin

# Hostname Configuration
KC_HOSTNAME=localhost
